# Built binaries
bin/
/server

# Database (local copy - schema is in migrations, content comes from bundles)
progress/go-learn.db
*.db

# Development artifacts (codegraph, cursor, omo state)
.codegraph
.cursor
.omo

# Learning records (contains PII; scrub before push)
learning-records/

# Personal config (contains PII)
AGENTS.md
MISSION.md

# Wrangler config (contains account-specific IDs and domain)
wrangler.toml

# Environment
.env
.env.local

# OS
.DS_Store

# Wrangler/Workers artifacts
.wrangler/
build/app.wasm
build/worker.wasm
build/*.wasm
node_modules/

# Generated content (build artifact, not source)
internal/content/content_generated.go
coverage.out
