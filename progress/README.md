# Progress tracking

Quiz answers and all lesson content live in **`go-learn.db`** (SQLite) in this folder.

Start the app with `make serve` from the repo root, then open **http://127.0.0.1:4173/**

Import content with `make import` from `content-bundles/`.
