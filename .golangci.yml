version: "2"
linters:
  default: standard
  enable:
    - errcheck
    - govet
    - gosec
    - staticcheck
  exclusions:
    paths:
      - "(.+)_test\\.go$"
formatters:
  enable:
    - gofmt
    - goimports
